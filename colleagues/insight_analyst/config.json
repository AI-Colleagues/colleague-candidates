{
  "configurable": {
    "ai_model": {
      "type": "string",
      "enum": [
        "openai:gpt-4.1-mini",
        "openai:gpt-5.4-mini"
      ],
      "title": "Model",
      "description": "The AI model to use for the agent.",
      "default": "openai:gpt-5.4-mini"
    },
    "batch_size": {
      "type": "integer",
      "title": "Batch Size",
      "description": "Number of responses to process per batch.",
      "default": 25
    },
    "per_turn_batch_budget": {
      "type": "integer",
      "title": "Per-Turn Batch Budget",
      "description": "Maximum number of responses to process in a single turn.",
      "default": 1000
    },
    "quotes_per_theme": {
      "type": "integer",
      "title": "Quotes Per Theme",
      "description": "Number of representative quotes to surface per theme.",
      "default": 3
    },
    "research_objective": {
      "type": "string",
      "format": "textarea",
      "title": "Research Objective",
      "description": "The research objective guiding the analysis.",
      "default": ""
    },
    "segment_variables": {
      "type": "string",
      "format": "textarea",
      "title": "Segment Variables",
      "description": "Variables used to segment the respondent data.",
      "default": ""
    },
    "seed_codebook": {
      "type": "string",
      "format": "textarea",
      "title": "Seed Codebook",
      "description": "Optional seed codebook to guide theme discovery.",
      "default": null
    }
  }
}
