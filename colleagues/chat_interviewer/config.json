{
  "configurable": {
    "ai_model": {
      "type": "string",
      "enum": [
        "openai:gpt-4.1-mini",
        "openai:gpt-5.4-mini"
      ],
      "title": "Model",
      "description": "The AI model to use for the agent.",
      "default": "openai:gpt-5.4-mini"
    },
    "system_prompt": {
      "type": "string",
      "title": "System Prompt",
      "description": "The system prompt for the agent.",
      "default": "You are a helpful assistant that can use widget tools to interact with the user. Use the provided tools to gather information and assist the user effectively."
    }
  }
}
