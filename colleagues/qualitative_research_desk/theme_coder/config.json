{
  "configurable": {
    "ai_model": {
      "type": "string",
      "enum": [
        "openai:gpt-4.1-mini",
        "openai:gpt-5.4-mini"
      ],
      "title": "Model",
      "description": "The AI model to use for the agent.",
      "default": "openai:gpt-5.4-mini"
    },
    "batch_size": {
      "type": "integer",
      "title": "Batch Size",
      "description": "Number of units to code per batch.",
      "default": 25
    },
    "per_turn_batch_budget": {
      "type": "integer",
      "title": "Per-Turn Batch Budget",
      "description": "Maximum number of recoding batches to run in a single turn.",
      "default": 1000
    }
  },
  "recursion_limit": 250
}
