{
  "configurable": {
    "ai_model": {
      "type": "string",
      "enum": [
        "openai:gpt-4.1-mini",
        "openai:gpt-5.4-mini"
      ],
      "title": "Model",
      "description": "The AI model to use for the agent.",
      "default": "openai:gpt-5.4-mini"
    },
    "batch_size": {
      "type": "integer",
      "title": "Batch Size",
      "description": "Number of responses to process per batch.",
      "default": 25
    }
  }
}
