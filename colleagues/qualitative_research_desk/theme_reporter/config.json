{
  "configurable": {
    "ai_model": {
      "type": "string",
      "enum": [
        "openai:gpt-4.1-mini",
        "openai:gpt-5.4-mini"
      ],
      "title": "Model",
      "description": "The AI model to use for the agent.",
      "default": "openai:gpt-5.4-mini"
    },
    "quotes_per_theme": {
      "type": "integer",
      "title": "Quotes Per Theme",
      "description": "Number of representative quotes to surface per theme.",
      "default": 3
    },
    "research_objective": {
      "type": "string",
      "format": "textarea",
      "title": "Research Objective",
      "description": "The research objective guiding the report.",
      "default": ""
    },
    "segment_variables": {
      "type": "string",
      "format": "textarea",
      "title": "Segment Variables",
      "description": "Comma-separated variables used to segment the respondent data.",
      "default": ""
    }
  }
}
