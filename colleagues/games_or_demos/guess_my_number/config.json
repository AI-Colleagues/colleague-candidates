{
  "configurable": {
    "ai_model": {
      "type": "string",
      "enum": [
        "openai:gpt-4.1-mini",
        "openai:gpt-5.4-mini"
      ],
      "title": "Model",
      "description": "The AI model to use for the game-master agent.",
      "default": "openai:gpt-5.4-mini"
    },
    "minimum": {
      "type": "integer",
      "title": "Minimum Number",
      "description": "Smallest valid number the human can guess.",
      "default": 0
    },
    "maximum": {
      "type": "integer",
      "title": "Maximum Number",
      "description": "Largest valid number the human can guess.",
      "default": 100
    }
  }
}
